{
  "scripts": {
    "prestart": "cd .. && npm run build",
    "start": "npx sls print --verbose"
  },
  "devDependencies": {
    "serverless": "^3.25.1",
    "serverless-assume-role": "file:.."
  }
}
